/**
 * Supabase client — stub until @supabase/supabase-js is installed.
 * Install: npm install @supabase/supabase-js
 * Then set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in .env
 */

export function getSupabase() { return null }
export function isCloudEnabled() { return false }
export default null
